
/**
 * Lets think
 * 
 * so lets say we create a 'code' cache for every ran program
 * this would mean we save the "non write" code for every program
 * this would just be saving the location of the pages or the page table ranges
 * or something similar
 */
